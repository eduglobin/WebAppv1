package com.eduglobin.library;

/**
 * Library domain — stub for Day 2.
 *
 * <p>Day 2 will implement:
 * <ul>
 *   <li>POST /api/v1/libraries — owner creates a library</li>
 *   <li>GET  /api/v1/libraries — student geo-search (lat/lon/radius via PostGIS ST_DWithin)</li>
 *   <li>GET  /api/v1/libraries/{id} — detail view with shifts and seat map</li>
 * </ul>
 */
public class LibraryPlaceholder {
    // Populated on Day 2
}
